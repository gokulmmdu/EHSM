<mvc:View controllerName="ehsmportal.controller.Login"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout">
    <Page id="page" title="{i18n>title}">
        <content>
            <FlexBox alignItems="Center" justifyContent="Center" height="100%" direction="Column">
                <items>
                    <Panel headerText="Safety Engineer Login" width="400px" class="sapUiResponsiveMargin">
                        <content>
                            <VBox class="sapUiSmallMargin">
                                <Label text="Employee ID" labelFor="inputEmpId"/>
                                <Input id="inputEmpId" placeholder="Enter Employee ID" value="{loginModel>/EmployeeId}"/>
                                <Label text="Password" labelFor="inputPassword"/>
                                <Input id="inputPassword" type="Password" placeholder="Enter Password" value="{loginModel>/Password}"/>
                                <Button text="Login" type="Emphasized" press="onLoginPress" class="sapUiSmallMarginTop" width="100%"/>
                            </VBox>
                        </content>
                    </Panel>
                </items>
            </FlexBox>
        </content>
    </Page>
</mvc:View>
